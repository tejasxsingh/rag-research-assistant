services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - CHROMA_DIR=/app/chroma_data
    volumes:
      - chroma_data:/app/chroma_data

volumes:
  chroma_data:
